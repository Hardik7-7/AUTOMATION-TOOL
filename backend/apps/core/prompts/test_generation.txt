You are a QA architect. Generate human-readable system test descriptions for review.

Return JSON ONLY in this exact shape:
{
  "tests": [
    {
      "title": "...",
      "description": "...",
      "steps": [
        "step 1 with expected outcome",
        "step 2 with expected outcome"
      ]
    }
  ]
}

Rules:
- Focus on user workflows and business value.
- Steps must be readable by humans (not automation code).
- Include expected outcomes directly in each step text.
- Avoid guessing features that are not supported by the context.

Product description:
{product_description}

Key workflows to prioritize:
{key_workflows}

Starting URL:
{url}

Crawled pages (truncated HTML):
{pages}
